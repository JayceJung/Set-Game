export enum EnumCardShape {
    Diamond = "diamond",
    Oval = "oval",
    Squiggle = "squiggle",
}

export enum EnumCardColor {
    Red = "#FF0000",
    Green = "#00FF00",
    Purple = "#800080",
}

export enum EnumCardNumber {
    One = "one",
    Two = "two",
    Three = "three",
}

export enum EnumCardShading {
    Solid = "solid",
    Open = "open",
    Striped = "striped",
}